# Memory — Empowr Members

## Current Status

- **⚡ 2026-08-18 (session 4): mobile audit done — PR #4 OPEN, NOT MERGED; PR #3 (perf) MERGED as `108e6bb`.** Ran the multi-viewport audit (320/360/375/414/768) across every page logged out / as member / as admin. **The real bug was `AdminHeader`** — it never got session 2's header fix (kept `px-6`, always-visible "Members Admin" wordmark, 4 nav items at `gap-5`), which forced the layout viewport to **356px and made every admin page scroll sideways at 320px**, wordmark colliding with the nav. That is the staff check-in surface used on a phone at the door. Fixed to match Public/Member headers; measured `innerWidth` 356→320, `canScroll` true→false. Also: session-detail `<li>` was `flex-wrap` so every Book button wrapped to its own line (page 2476→2052 CSS px at 375, -17%); `/sessions` age filter's `ml-auto` stranded the control right-shoved on its own mobile row (→ `sm:ml-auto`); tap targets 16-34px → 40-42px; all 16 `<main>`s used flat `px-6` vs headers' `px-4 sm:px-6`, so content sat inboard of the header. **CSS-only, no logic changes.** Public pages verified on the real deploy preview (no h-scroll at 320/375, rows uniform 68-73px); **the admin-header fix was verified locally at 320px on the identical built code, not on the preview** (admin routes need auth the preview can't easily grant). **Cleared, not bugs:** no overflow on any public/member page, no sub-12px text, the admin register table already had `overflow-x-auto` and only looked cut off because the page under it overflowed; remaining sub-44px hits are inline text links, remaining "clipped" items are native `<select>` closed states.

- **⚡ 2026-08-18 (session 3): performance pass — PR #3 MERGED 2026-08-18 as `108e6bb` (was open at time of writing).** The platform felt slow; the database was never the cause (all hot paths indexed, advisor clean, dataset is 4 offerings / 51 occurrences / 3 bookings). The catalogue was uncacheable by design (`force-dynamic` → `private,no-cache,no-store`, `Netlify Durable: fwd=bypass`), so every visit paid a function invocation plus a transatlantic round trip. Fixed with a cookie-free public client (`lib/supabase/public.ts` — `server.ts` reads `cookies()`, a dynamic API, which makes any page uncacheable), `unstable_cache` under one `catalogue` tag, and `revalidateCatalogue()` in all 12 catalogue-affecting admin write handlers. **Measured: `/sessions/[slug]` 586ms → 30ms p50.** `/sessions` only 344→274ms and **that is its ceiling** — it reads `searchParams` so it renders per request; going further means client-side filters. Auth round trips also cut (middleware early-return, React `cache()`, parallelised booking pages). **`getClaims()` DECLINED by the user** — ES256 keys would allow local JWT verification but `jwt_exp=3600` means a revoked session stays valid up to an hour; wrong trade for a payments platform, do not re-propose without raising that. **🔴 Gotcha: `export const revalidate` is INERT on a `[slug]` route without `generateStaticParams`** — builds and typechecks fine, silently stays dynamic; read the build legend. **Revalidation e2e-verified with a control window; the purge is async ~5s** and `ttl` is not a usable signal (absent on a miss) — use `"Next.js"; fwd=miss` + an `age` reset. Items 3-4 of the perf plan (streaming/`loading.tsx`, `next.config` tuning, asset cache headers) NOT done — deprioritised below mobile and bookings. `framer-motion` is unused but deliberately KEPT (costs users zero bytes since nothing imports it; removal is hygiene, not performance).
- **✅ 2026-08-18 (session 2): the live-mode Stripe smoke test has been RUN, not just unblocked.** Booked + paid £7 for Skate Jam through the real UI as `admin@pecuvate.com`: booking confirmed, ticket page + QR rendered, admin check-in marked attended and was idempotent on reload. **Skate Jam is left `active=true`** — deliberately not reverted here; user is deactivating it themselves via `/admin/offerings`, not yet confirmed done. Along the way: found + fixed a waiver bug (`emergency_contact_*` wrongly required for an adult self-signer, commit `b9f4b96`); found + fixed a real cross-app bug where the payment fired a genuine Empowr Heroes donation email (Heroes+Members share one live Stripe account; fixed in `empowr-heroes-nextjs`, see that project's memory.md); found (not yet fixed — Pro-plan-gated, deferred) that Netlify's `functions_region` is `us-east-2` against Supabase's `eu-west-2`; ran a Playwright mobile audit and fixed three real issues (**⚠️ NOT SUFFICIENT — as of session 3 the user reports the platform still does not feel responsive on mobile/small screens; a proper multi-viewport audit is outstanding, and re-running the same 375px overflow check is not enough, it already missed the header-wrap issue once**) (undersized "Book" touch targets, a 10px ticket label, and header/nav wrapping on narrow screens — commits `cf5377f`, `b648ba7`).
- **✅ 2026-08-18: PR #2 (tier-1 waiver decoupling + PassKit removal, bundled) merged to `main` and confirmed LIVE in production** — checked `members.empowrcic.org/ticket/<fake-id>` directly post-merge and got the app's own branded not-found page, not Netlify's generic 404, proving the deploy actually picked up the new routes rather than just trusting a green merge. Nothing left blocking launch except the two deliberate items below.
- **✅ 2026-08-17 (later session): PassKit removed entirely, replaced with an in-house QR ticket page.** No more Apple/Google Wallet integration — `/ticket/[bookingId]` is a new **unauthenticated** page (deliberate, user-confirmed: someone other than the account holder can show the ticket on their own phone) with a QR that encodes `/admin/checkin/[bookingId]`, scanned via native camera (not in-page — `netlify.toml` ships `Permissions-Policy: camera=()`). New admin check-in flow at that route (inherits the existing `ADMIN_EMAILS` gate, no new kiosk auth) is a read-only lookup + explicit "Mark attended" button, not an auto-mark-on-scan. `per_run` course bookings deliberately can't be marked attended (no per-week attendance concept in the schema) — surfaced with a note, not silently allowed. No DB migration — `passkit_pass_id`/`passkit_venue_id` left as harmless dead columns, drop in a later standalone migration. `lib/passkit.ts` deleted; `planning/passkit/CONTEXT.md` kept as historical record. **E2E-verified for real**: booked through the real UI to a real Stripe Checkout redirect, self-signed a `checkout.session.completed` webhook event, confirmed the booking flipped to `confirmed`, loaded the ticket in a fresh cookie-less browser context (proving the no-login claim), checked in via `/admin/checkin`, confirmed idempotent re-check, confirmed a cancelled test booking's ticket shows "no longer valid" instead of a ticket. Full detail: DEVLOG 2026-08-17 (later session entry).
- **✅ 2026-08-17: the waiver gate blocker is FULLY RESOLVED — PR #1 can merge.** `mem_waiver_consents` built (migration `20260817152231`) — decoupled gate, `revoked_at IS NULL AND (expires_at IS NULL OR expires_at > now())`, never touches `form_versions` or `waiver_responses` again. **Scope changed from the original 2026-08-10 design**: Waivers independently shipped a 3-year retention window on `waiver_responses`/`departure_consents` (2026-08-13/14, their own evidence-retention reasons) which made tier 2 (session-dated write at booking) redundant — dropped from scope. Tier 1 was still built anyway, not for retention but to stop the booking gate being hostage to a table/policy Members doesn't own, and to kill the `form_versions.active` landmine (any wording fix un-covering everyone instantly) that retention never touched. `checkWaivers()` is now two-tier: `mem_waiver_consents` primary, legacy email/name match against `waiver_responses` as fallback (still needed — it's what recognises someone who signed on the standalone `waiver.empowrcic.org` app before ever touching Members), with the fallback lazily backfilling a consent row. **Per-booking departure consent also built**: new `mem_participants.default_travel_method` (migration `20260817152232`) pre-fills a per-booking toggle; the 5 `confirm_*` affirmations always start unchecked and must be re-given every booking (2026-08-10 decision); writes into Waivers' own `departure_consents` table, not a new one. The old one-time `consent_unaccompanied_departure` write from `submitWaiver()` is retired (always `null` now). **E2E-verified against the real DB and a real Stripe Checkout session** — see DEVLOG 2026-08-17 for the full proof chain (queried `mem_waiver_consents`/`departure_consents` directly, didn't just trust `checkWaivers()`'s return value). PR #1's "only need to do this once" copy is no longer false — left as-is on `/waiver`. **Nothing schema/gate-related blocks launch anymore** — what's left (flipping offerings active, the live-mode Stripe smoke test) is launch sequencing, not a technical blocker. Full detail: DEVLOG 2026-08-17.
- **🔧 Reusable gotcha from this build**: on this machine, `Get-Content -Raw` in PowerShell does NOT return a plain string when piped into `ConvertTo-Json` — it carries pipeline metadata that inflated a 1870-byte SQL file into a 3.8MB JSON payload (413 from the Supabase Management API). Fix: `[System.IO.File]::ReadAllText($path)`. Relevant any time raw SQL is sent via the Management API curl/PowerShell path (see [[reference_supabase_management_api_sql]]).
- **📝 Applying a migration via the raw Management API `/database/query` endpoint does NOT register it in `supabase_migrations.schema_migrations`** — `dump-ledger.mjs` won't pick it up until you also `INSERT` a row into that table by hand (version/name/statements, statements = one array element with the full SQL text). Discovered because `dump-ledger.mjs` reported "0 written" right after a migration that had visibly applied to the live schema.
- **SUPERSEDED by the above — kept for history:** 2026-08-10: the retention half of the blocker below is FIXED at the database. Migration `20260810114838` applied: `delete-old-waivers` keys on `session_date`, departure consents get parity, `NOT EXISTS` replaces `NOT IN`, 13-month backstop added. **Also flipped `mem_participants.person_id` from `ON DELETE NO ACTION` to `SET NULL`** — the job ends with a mass `DELETE FROM people`, and pg_cron runs it as one transaction, so the first participant PR #1 linked would have aborted the whole job and **silently stopped all retention**. That was one deploy away from firing. Migration lives in `empowr-cic-workspace`, not here.
- **🛑 PR #1 is STILL ON HOLD — do not merge.** Its "you only need to do this once" copy stays false until tier 1 exists. The remaining work is entirely app-side: tier 1 table, tier 2 session-dated write at booking, per-booking departure consent, and repointing `checkWaivers()`.
- **📐 Tier 1 is designed, not built — don't redesign it.** `mem_waiver_consents`, one row per participant (adding a child tops up rather than forcing a re-sign). **Load-bearing decision: the gate checks `revoked_at IS NULL AND (expires_at IS NULL OR expires_at > now())`, NOT "consent against the active form version"** — that defuses the `form_versions.active` landmine where a typo fix would un-cover every member at once. `form_version_id` kept for audit only. **Must ship explicit `GRANT`s** or every PostgREST call fails `42501` on this project.
- **🧭 Departure consent is PER-BOOKING, not standing** (user decision 2026-08-10). A standing "my child may leave alone" consent is not equivalent in risk to a standing risk waiver — it is a judgement a parent may make differently on a dark December evening, and it carries a specific `travel_method`. Asked at each booking, pre-filled from an updatable default in the member profile.
- **🏗 Build paused 2026-08-10 for a shared-database architecture review, then cleared to proceed.** See `_config/guides/supabase-structure-blueprint.md` (DRAFT). Verdict: nothing blocks tier 1 — it is `mem_`-prefixed, touches no cron job and no `auth.users` trigger, and its only cross-app edge is to `form_versions` (append-only, no purge job).
- **🛑 2026-08-09 (retention half now RESOLVED above): the launch blocker is the waiver retention conflict — PR #1 is ON HOLD, do not merge.** `delete-old-waivers` (pg_cron, daily 03:00) deletes `waiver_responses` older than 24h, while `checkWaivers()` requires one to exist. So a member who signs today is blocked from booking tomorrow: **"sign once" is impossible against that table**, and the `/waiver` page built on `feat/in-app-waiver` promises exactly that in copy. A second defect in the same branch: it writes `session_date` = the *signing* date, but the Waivers staff check-in portal filters `session_date = today`, so a member signing ahead is invisible at check-in. **Agreed design (not built)**: two tiers — account-level standing consent in a new Members-owned `mem_` table immune to the purge, plus a session-specific step at booking writing a properly-dated `waiver_responses` row. **Next decision needed from the user before any build**: the purge keys on `submitted_at`, not `session_date`, so a per-visit row written at booking time dies before the session — either change the purge to `session_date < current_date` (one line, needs the Waivers owner) or have Members write the row on the session morning via a scheduled job (~double the work). About two-thirds of PR #1 is reusable. Full detail: [[project_empowr_waiver_gate_blocker]], DEVLOG 2026-08-09.
- **✅ 2026-08-09: waiver copy now mirrors the standalone form verbatim** (committed on `feat/in-app-waiver`). All three agreement cards carry the canonical wording, relationship is a select with the canonical eight options, `links.ts` gained `photographyConsent`, and **photo consent is now REQUIRED** to match. Also removed the emergency-contact backfill onto `mem_participants` — the standalone form promises "contact numbers are not stored on file". Separately flagged: mandatory photo consent is a shaky "freely given" basis under UK GDPR, inherited deliberately.
- **✅ 2026-08-05: catalogue seeded** — 4 venues (all with real `passkit_venue_id`), 4 offerings, 51 occurrences to 2026-11-01, capacity 25 set on `mem_venues.default_capacity` (so raising it later is 4 rows, not 51). **All four offerings are deliberately `active = false`** and must stay that way until the waiver architecture is settled. `PASSKIT_ENABLED` kill switch added, off unless explicitly `"true"` — DRAFT-mode passes carry a "Test Pass - Not for Commercial Use" stamp and expire in 48h.
- **📁 2026-08-06: migrations left this repo.** `src/supabase/migrations/` is gone. All 22 for the shared `empowr-cic` database now live in `Empowr CIC/supabase/migrations/`, generated from the migration ledger by `dump-ledger.mjs`. **Do not re-create `src/supabase/` here** — after applying any migration, re-run the dump and commit it there instead. Why: three projects share this database and per-app filing meant none could rebuild it (7 of 22 migrations had no file anywhere). Nothing was lost, and the 22 lines of schema rationale in `members_initial_schema.sql` were backfilled into the ledger first. Going forward, apply migrations **with their comments included** — the ledger stores only what was sent.
- **⚠️ CORRECTED 2026-08-05 — the bullet below is wrong on its central claim.** `mem_hold_bookings()` treats a NULL capacity as **unlimited**, not unbookable (`if v_capacity is not null and ...`, with an inline comment saying exactly that). Seeding was never blocked on capacity; the hold was self-imposed. Capacity is a safeguarding concern (overselling a room, coach-to-child ratios), not a technical gate. Catalogue was seeded 2026-08-05 with an interim capacity of 25.
- **🔍 Narrowed 2026-07-30 (SUPERSEDED, see above): Q6 (real-timetable seeding) is not a data-from-scratch problem — it's a capacity-numbers problem.** User asked whether the Empowr CIC KB (`entities/sessions.md`, as_of 2026-07-22, plus the older `sources/operations/sessions-booking.md`) could substitute for a fresh ask to Jasmine. It largely can: both pages have real venue/day/time/price/duration for every recurring session and dated 2026 course ranges. Confirmed with the user: the schedule has been stable 3+ years (so the KB's own "times/dates change" caveat is low-risk here), and **all sessions — including courses — are migrating fully from Wix to Members** (so courses aren't a separate ownership question). **What neither KB page has, at all: capacity/attendee limits per session.** `mem_hold_bookings()` needs a per-occurrence capacity to lock against — nothing can be seeded as a real bookable slot without it, and it's a genuine operational/safeguarding fact (room size, coach ratios, under-18 ratios) to get from Jasmine/Shaun, not to infer. **Next session**: once capacity numbers land, draft the full seed list (venues → offerings → recurring occurrences) from the KB data for a quick confirm pass instead of building from scratch — unblocks seeding → the live-mode Stripe smoke test → public launch.
- **✅ Done 2026-07-30:** instrumented with PostHog analytics (Variant B: cookieless `on_reject` + consent banner, same pattern as EELA) ahead of public launch — `PostHogProvider.tsx` + `CookieConsentBanner.tsx`, `analytics_sites` Supabase row created. No CSP change made deliberately (none exists yet; site runs Stripe + PassKit, first-time CSP needs its own audit). Not yet verified against real PostHog Live Events — zero real traffic pre-launch. A separate fleet-wide fix landed in `PostHogProvider.tsx` the same day (commit `1725d80`): `capture_pageview` was `true`, which disables posthog-js client-side route-change capture entirely. **This mattered more here than on any other site** — the booking and membership flows are almost entirely client-side navigation, so essentially the whole funnel would have been invisible from day one of real traffic. Nothing was lost (zero traffic so far), but it would have silently wasted the launch. Now `'history_change'`, verified in the deployed bundle. Full detail: [[project_empowr_members_analytics]], DEVLOG 2026-07-30.
- **✅ Resolved 2026-07-29 (spec risk #5, was a Phase 1 launch gate):** legal policy links wired into the site for the first time — previously zero legal links existed anywhere in the app. No new Sanity content needed: the org privacy policy (v1.2, updated 2026-07-28, `platform: "org"`) already covers programme-booking DOB/health data under "Programme Bookings" since Members is legally the same entity as the rest of Empowr CIC. Added the same `/legal/:slug` → `legalhub.pecuvate.com/share/empowr/org/:slug` Netlify proxy Main Site uses, `lib/links.ts` gained `privacyPolicy`/`termsAndConditions`/`riskWaiver`, new `Footer.tsx` mounted once in root `layout.tsx` (covers all route groups). Clean build verified; proxy target confirmed live (200). Full detail: DEVLOG 2026-07-29.
- **🔄 BEING REVERSED — decision 2026-08-18 (session 3): self-serve cancellation/transfer/reschedule is coming BACK, after the policies are amended to v1.2.** The removal below was correct against Terms & Conditions v1.1 / Programme Policies v1.1 ("All bookings are final"), both still v1.1 as of 2026-08-18. The user's reasoning for reversing is not a contradiction: the original no-cancel decision was driven by **admin workload** — handling requests by email would be unmanageable — and self-service *eliminates* that workload, so the original rationale now argues for the change. User explicitly rejected a request-and-approve middle path in favour of true self-serve. **Sequence: amend the 2 Sanity policies → update the KB → re-run `/sync-kb` (or the CRM chat widget will quote the OLD policy to real customers) → then code.** Zero DB migrations needed — `mem_booking_status` already has `cancelled/credited/refunded`, `mem_credits` exists, `mem_offerings.transferable` exists with an admin toggle already rendered (0 of 4 offerings currently flagged). Full blast radius in [[project_empowr_policies]].
- **✅ Resolved 2026-07-21 (was flagged 2026-07-20) — SUPERSEDED by the bullet above:** self-serve cancellation removed entirely — `lib/cancellation.ts`, the `POST /api/bookings/[id]/cancel` route, and the member-cancellation email are deleted; `BookingsList`/`PolicyNotice`/the confirmation email now all state bookings are final by default, discretionary exceptions via enquiries@empowrcic.org only. The only remaining refund/credit path is the admin occurrence-cancel route (Step 8) — already an explicit human decision, so it needed no functional change, just a stale-comment fix. Verified via clean `next build`; not verified live in-browser (dev DB has no seeded catalogue). Full detail: DEVLOG 2026-07-21.
- **🛑 SUPERSEDED 2026-08-17 — PassKit removed entirely, see Current Status above. Kept below for history only, do not build against this.** PassKit Track A: COMPLETE (2026-07-21) — built, e2e-proven live, and deployed. Full details, all real object IDs, and every REST-vs-gRPC gotcha discovered are in `planning/passkit/CONTEXT.md` — **read that before touching PassKit code again, do not rediscover any of it**. Summary: JWT auth works (claim `uid`, header `Bearer`, 60s-backdated `iat`); `lib/passkit.ts` has `createPassKitVenue`/`issueSessionPass`/`voidPass`/`passInstallUrl`, all never-throw. `mem_bookings.passkit_pass_id` + `mem_venues.passkit_venue_id` columns added; venue creation wired into `POST /api/admin/venues`. Stripe webhook issues one pass per booking row on first-confirm (`issuePassesForSession` in `lib/notifications.ts`; per-run bookings span the whole run via min/max of the run's occurrence timestamps); confirmation email renders an "Add to Wallet" button per participant with an issued pass; admin occurrence-cancel voids the pass after a confirmed booking's refund/credit succeeds. **Step A8 live e2e proof passed 2026-07-21**: self-signed a real Stripe webhook event against the running dev server, confirmed a real PassKit ticket was issued and persisted; read the real confirmation email via Gmail MCP and confirmed the wallet link; authenticated a real admin session via a temporary `ADMIN_EMAILS` addition + `generateLink`/`token_hash` (reusable pattern for future admin-route e2e proofs without needing an admin inbox) and confirmed the real admin-cancel route voided the pass (ticket 404s after). All seeded rows + test users cleaned up, zero leftovers. **Deployed**: commit `6d8f6b5` pushed to `main` 2026-07-21, PassKit env vars already confirmed live on Netlify beforehand. Key facts: pass install URL is `https://pub1.pskt.io/{ticketId}` (client-constructed, behind `passInstallUrl()`); `DeleteTicket` is a genuine hard delete; delete endpoints use different body field names per resource (`id` for venues, `ticketId` for tickets); **new fact from A8**: `DELETE /eventTickets/venue` refuses once a ticket's been issued against it (auto-created Event holds a reference, no Event-delete endpoint exists) — one test venue left as a harmless named orphan. **Blocked**: Apple Wallet passes need a Pass Type ID + cert from an Apple Developer Program membership — in progress, not ready yet; Google Wallet unaffected, not yet installed on a real phone (worth doing post-deploy, template rendering can't be asserted from code). Track B (membership pass) still blocked on Phase 2 — do not build.
- `ADMIN_EMAILS` now has two admins: `tech@pecuvate.com` and `admin@pecuvate.com` (added 2026-07-21, same three-leg local/vault/Netlify update path as the PassKit keys).
- **Live-mode Stripe smoke test: confirmed blocked 2026-07-29**, not just "not yet run" — checked production Supabase directly, catalogue is completely empty (0 `mem_venues`/`mem_offerings`/`mem_occurrences`/`mem_course_runs`). Same Q6/Jasmine real-timetable gap as Step 3. User decision: defer the smoke test until real seeding happens rather than build a throwaway test listing on the live site. **Also found a leftover `mem_accounts` row** (id `83602522-1602-421b-b034-defeeb448cca`, created 2026-07-21) from the PassKit Track A8 e2e session — that DEVLOG entry claimed "zero leftover rows verified" but this one row survived. User decision 2026-07-29: leave it as-is for now, don't delete.
- Phase: **Phase 1 built and deployed — site is NOT yet publicly launched** (confirmed 2026-07-14; correcting earlier phrasing that conflated "Stripe in live mode" with "launched"). Steps 2, 3-pages, 4, 5, 6, 7, 8 DONE. **Step 9 (Verify + go live) in progress as of 2026-07-12**: full e2e pass (signup->book->cancel->credit) 6/6 PASS via delegated agent, zero app bugs; `/pre-deploy-security` 0 FAILs; `/netlify-supabase-check` clean after removing a dead `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` stub. **Stripe credentials switched to LIVE in production** (2026-07-12) — `MEMBERS_STRIPE_SECRET_KEY_LIVE`/`MEMBERS_STRIPE_WEBHOOK_SECRET_LIVE` vaulted, `sync-to-netlify.ps1` repointed, Netlify `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` set to live values scoped to the `production` context only (previews/branch-deploys still on TEST), production rebuilt + confirmed serving. This means real bookings *would* charge real cards if anyone booked — but the site hasn't been announced/launched yet, so **no real member bookings or payments have occurred**. **Remaining before launch**: ~~privacy policy live via LegalHub~~ — resolved 2026-07-29, see Current Status above; a live-mode smoke test (blocked on empty catalogue as of 2026-07-29, not just "not yet run"), registries update, phase-1 doc compression. Step 3 real-timetable *seeding* still blocked on Q6, but narrowed 2026-07-30 to specifically capacity numbers (see Current Status above — schedule/venue/price data is already derivable from the KB) — the Step 8 admin area is where that seeding will happen once unblocked; prod /sessions shows "timetable being finalised" until then. Business-rule values live as constants in `src/lib/business-rules.ts`. shadcn still deferred — brand-token primitives in `components/ui/form.tsx`. Deployed at https://members.empowrcic.org (Netlify site `76f903e4-3795-406a-9478-34be6b0ed015`) but pre-launch. **Deploys via GitHub push → CI**; `publish = ".next"` — CI resolves it relative to base (never CLI-deploy). Env-var updates to an *already-live* Netlify site are a separate path from that — see [[feedback_netlify_env_update_api]].
- Step 8 shape (for Step 9 to reuse): `(admin)/admin/*` allowlist-gated via `lib/admin.ts` (`ADMIN_EMAILS`, checked in the layout + every `/api/admin/*` route — middleware only session-guards `/admin`). Full CRUD for venues/offerings/occurrences/course-runs, register view, and cancel-occurrence (bulk refund/credit reusing Step 7's primitives in a loop, folded per-account into one notice email, deliberately skipping the member-facing 48h/non-refundable policy gate since Empowr-initiated cancellation isn't subject to it). e2e-verified incl. a folded multi-child email and FK-blocked venue delete. **Real bug found+fixed**: never import from `lib/catalogue.ts` (has `import "server-only"`) into a client component — it silently breaks the whole bundle, not just that page; offering-type constants now live in client-safe `lib/offering-types.ts`.
- Step 7 shape (for Step 8/9 to reuse): `lib/cancellation.ts` = pure `evaluateCancellationPolicy(refundPolicy, startsAt)`, shared by the page's render-time estimate and the API's authoritative check. `POST /api/bookings/[id]/cancel` = atomic claim (`confirmed`→`refunded`/`credited` guarded by `.eq("status","confirmed")`) *before* the Stripe partial-refund call or `mem_credits` insert, rolled back to `confirmed` on failure. Refund is partial against the booking's own `stripe_payment_intent_id` (safe even though a multi-child booking shares one PI per Checkout session — each booking only ever refunds its own `price_paid_pence`).
- Step 6 shape (for Steps 7/8 to reuse): emails via Resend, sender `Empowr CIC <members@empowrcic.org>` (Resend-verified; same as auth SMTP), reply-to general@empowrcic.org. `lib/email.ts` = client singleton + `sendEmail()` that **never throws** + brand-inlined primitives (`emailLayout`/`panel`/`detailRow`/`ctaButton`/`esc`). `lib/emails/*` = 3 **pure** builders (confirmation, cancellation refund|credit, occurrence-cancelled) returning `{subject,html}`. `lib/notifications.ts` = orchestrators (recipient via account→`auth.admin.getUserById`): `sendBookingConfirmationForSession` (wired into the Stripe webhook, first-confirm only, failure-swallowed), and `sendBookingCancellationEmail`/`sendOccurrenceCancelledEmail` for Steps 7/8 to call. `membersUrl()` in links.ts for absolute email URLs. **Deploy-time TODO: confirm `RESEND_API_KEY` is actually pushed to the live Netlify site** — it IS in `sync-to-netlify.ps1 $siteVarMap` for the Members site (76f903e4…, line 133) but Step 5 only pushed the two Stripe keys; the webhook's Resend call fails-soft (logs, no email) if the key is missing in prod, so run the sync before relying on confirmation emails going live.
- Step 5 shape (for Steps 6/7 to reuse): `POST /api/bookings` holds then creates a **card-only** Checkout session (`getStripe()` in `lib/stripe.ts`; one line item per participant at the snapshotted `price_paid_pence`), links `stripe_checkout_session_id` onto the holds and **extends `expires_at` past the session's 31-min expiry by `HOLD_GRACE_MINUTES` (10)** so a late payment beats the pg_cron sweep; session-creation failure releases the holds inline; returns `{ checkout_url }`, `BookingForm` redirects. Webhook `POST /api/webhooks/stripe` (async-verified, idempotent): `completed`(paid)→`confirmed` + PI persisted + `expires_at` cleared (paid-for-released-holds logged loudly for manual refund — no auto-refund until Step 7); `expired`→release; bad/missing sig→400. `/book/confirmation?session_id=` reads own bookings by session id + `AutoRefresh` polls ~20s. Stripe **customer created+persisted per account on first pay** (`mem_accounts.stripe_customer_id`) — Phase 2 Billing reuses it.
- Step 4 shape (for Step 7 to reuse): holds via `mem_hold_bookings()` RPC (row-locked, service-role only; inline expired-release; price snapshotted into `price_paid_pence`); expired pendings → `cancelled` via pg_cron `members-release-expired-pendings` (every minute); waiver gate in `lib/waivers.ts` (active form version + `skater_names` name match, persists `person_id`, fails closed, blocks with 409 + waiver link). Gotcha: service_role can't DELETE from `people`/`waiver_responses` — waiver-row test cleanup goes via SQL.
- Stripe env wiring (2026-07-10): prod webhook endpoint `we_1TraTSCpJGJ55gu5LdKiZb3e` (TEST) → members.empowrcic.org/api/webhooks/stripe; vault `MEMBERS_STRIPE_WEBHOOK_SECRET` = that endpoint's secret; `.env.local` `STRIPE_WEBHOOK_SECRET` = a LOCAL self-signed secret (not the prod one). `sync-to-netlify.ps1 $siteVarMap` Members entry has both Stripe rows; push per-key (bulk POST 422s when 5 vars already exist). **Stripe CLI login key expired** — `stripe listen` 401s; local webhook e2e done by self-signing events with `generateTestHeaderString`; `rk_test` restricted key can't run `listen` either.
- Step 2 shape (for later steps to reuse): auth = password + magic-link tabs (`shouldCreateUser: false` on OTP; no password-reset flow — magic link is recovery); middleware Pattern 1 guards /account /bookings /book /membership /admin; all writes via zod-parsed API routes with service client scoped to `getAuthedAccount()` (lib/auth.ts); e2e pattern = admin-createUser (email_confirm) + Playwright, cleanup deletes user (cascades).
- 2026-07-08 kickoff session: e2e signup test PASSED end-to-end (signup 200 → Resend SMTP email delivered to inbox in 2s from members@empowrcic.org → confirm link verifies user → mem_accounts trigger fires; test users deleted after). Found + fixed a Phase 0 defect: `mem_` tables had **no table-level grants** (project default ACL is hardened — new tables get no DML for API roles); `members_table_grants` migration applied and verified (service_role full DML, anon catalogue-only, authenticated + own-row tables). src/.env.local written; project registered in the vault pipeline (MEMBERS_* keys seeded; consolidate/pull/sync scripts all carry a members entry). Also fixed sync-to-netlify.ps1 (broken vault read → get_all_secrets RPC).
- Complete: plan, MWP scaffold, ADRs, GitHub remote + registries, execution plans for phases 0–4, Phase 0 (brand, schema, auth, Netlify + domain)
- Leaked-password protection Pro-plan-gated (accepted). Gmail note: connected Gmail connector reads the teams@empowrcic.org mailbox, not tech@pecuvate.com — use teams+ plus-addresses for future e2e email checks.

## Key Decisions

- Stripe test/live key handling (agreed 2026-07-09): vault holds TEST values under `MEMBERS_STRIPE_SECRET_KEY`/`MEMBERS_STRIPE_PUBLISHABLE_KEY` (local dev pulls these); at Step 9 go-live, add `_LIVE`-suffixed vault entries via the same .env.shared intake and point the members entry in sync-to-netlify.ps1 `$siteVarMap` at them — app code reads the unsuffixed env var everywhere; webhook secret gets the same twin treatment

- Domain: members.empowrcic.org (dashboard.empowrcic.org reserved for Empowr Dashboard)
- Database: existing `empowr-cic` Supabase project — waiver tables live there, so the waiver gate is a table join
- Payments: Stripe Checkout (one-off) + Billing (memberships); account = the shared **Empowr CIC Stripe account** (org account, currently only used by Heroes — CONFIRMED 2026-07-08). Members gets its own API keys (`MEMBERS_STRIPE_*`) in that account at Step 5; never reuse or rename Heroes' keys
- EELA stays discovery-only; this project is the transactional layer
- All new tables `mem_` prefixed; writes via service client only

## Preferences

- Make reasonable implementation decisions without asking; only block on genuine business questions (entitlements, Stripe account)

## Parked work waiting on this project

- `feat/my-account-nav` (Empowr Main Site) and `feat/members-account-notice` (Empowr EELA) — merge at Phase 4 cutover; set `membersUrl` in Main Site links.ts first

## Pre-Close Checklist

- [ ] Update Current Status to reflect what changed this session
- [ ] Record any new decisions in Key Decisions (ADR in planning/decisions/ if significant)
- [ ] Note any outstanding work that was not completed
